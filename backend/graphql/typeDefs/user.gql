type User {
  _id: ID!
  name: String
  email: String
  photo: String
}

input inputUserType {
  name: String
  email: String
}

type Query {
  userList: [User]
  user(_id: ID): User
}

type Mutation {
  addUser(body: inputUserType): User
}
